---
services:
  foo:
    lifecycle:
      install: echo installing foo-service
      run: echo foo-service
  bar:
    lifecycle:
      run: echo bar-service
  com:
    lifecycle:
      bootstrap: echo bootstrapping; exit 100
      run: echo com-service
  main:
    dependencies:
      - foo
      - bar
      - com
      - aws.greengrass.Nucleus
